<h1>Eat-Da-Burger!</h1>

<ol>
    {{#each burgers}}
        <li>
            <p>{{this.burger_name}}</p>
        </li>
    {{/each}}
</ol>

<form class="create-form">

    <div class="form-group">
        <input type="text">
    </div>

    <button type="submit">Submit</button>
</form>